<%= form_with model: @recipe_food, url: recipe_foods_path, method: :post, local: true, scope: :recipe_food do |form| %>
  <% if @recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@recipe_food.errors.size, "error") %> prohibited this food from being saved:</h2>

      <ul>
        <% @recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :recipe_id %>
    <%= form.text_field :recipe_id, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :food_id %>
    <%= form.text_field :food_id, class:"form-control" %>
  </div>

  <div>
    <%= form.submit, class:"btn btn-secondary" %>
  </div>
<% end %>
